<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.fairhand.supernotepad.view.ItemView
        android:id="@+id/iv_arrange_grid"
        android:layout_width="168dp"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:layout_marginTop="192dp"
        android:layout_marginEnd="4dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:left_icon="@drawable/ic_grid"
        app:left_text="网格"
        app:show_left_icon="true" />

    <com.fairhand.supernotepad.view.ItemView
        android:id="@+id/iv_arrange_timeline"
        android:layout_width="168dp"
        android:layout_height="52dp"
        android:layout_margin="4dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/iv_arrange_grid"
        app:left_icon="@drawable/ic_timeline"
        app:left_text="时间线"
        app:show_left_icon="true" />

</android.support.constraint.ConstraintLayout>
